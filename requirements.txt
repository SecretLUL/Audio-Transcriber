# Runtime dependencies
PyAudioWPatch>=0.2.12; sys_platform == "win32"   # WASAPI loopback (Windows)
PyAudio>=0.2.13; sys_platform != "win32"         # portaudio elsewhere
soundfile>=0.12.1        # WAV reading/writing
numpy>=1.24
scipy>=1.10              # polyphase resampling
Pillow>=10.0             # vector icon rendering in ui/icons.py

# Key storage uses the Windows DPAPI through ctypes and needs no extra
# library. Tkinter ships with the standard Python installation on Windows.
